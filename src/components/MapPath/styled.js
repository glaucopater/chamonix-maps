import styled from 'styled-components';

export const StyledWrapper = styled.div`
  display: block;

  .leaflet-container {
    height: 300px;
    width: 100%;
  }

  .leaflet-div-icon {
    background: transparent;
    border: none;
  }
`;
