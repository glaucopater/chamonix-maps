import styled from 'styled-components';
import { DEFAULT_PADDING } from '../../constants/';

export const StyledMapDetails = styled.div`
  padding: ${DEFAULT_PADDING};

  > h2 {
    font-size: 2rem;
    padding-bottom: 1rem;
  }
`;
